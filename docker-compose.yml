version: "3.9"

services:
  web:
    build: .
    container_name: django_app
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    env_file:
      - .env
    depends_on:
      - db

  db:
    image: postgres:16
    container_name: postgres_db
    restart: always
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: testserver
      POSTGRES_USER: testserver
      POSTGRES_PASSWORD: 23051988joswelj
    ports:
      - "5432:5432"

volumes:
  postgres_data:

# WARNIN AHORA DEBES PASAR TODO AL SERVER

